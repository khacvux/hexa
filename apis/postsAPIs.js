import AXIOS from "./axiosClient";

const url = 'posts'


export const getPostsAPI = async (data) => {
    try {
        let params = '';
        // data && (params = '')
    } catch (error) {
        
    }
}