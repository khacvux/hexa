import axiosClient from "./axiosClient";

export const signInAPI = async () => {
    try {
        // const users = await axiosClient.get('....');
        const users = 12312312;
        return users;
    } catch (e) {
        return console.log(e)
    }
}